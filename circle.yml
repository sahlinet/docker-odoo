machine:
  services:
    - docker
dependencies:
  override:
    - docker info
test:
  override:
    - sh build.sh
    - docker images
deployment:
  test:
    branch: master
    commands:
      - sh push.sh
